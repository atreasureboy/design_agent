# 思路：Agent Architecture CAD（企业级 Agent 架构设计工具）

> 状态：设计构想，持续演进。
> v17（2026-08-24）：从“展示架构知识”转向“主动辅助设计”。默认首页改为 Architecture Copilot：以 Brief 和现有蓝图为上下文，计算设计就绪度，按影响排序下一步任务，并解释建议原因与预期结果；缺失组件可沿合法父链一键加入。建议引擎下沉 Core，同时供 Web 与 MCP 使用，确保人机两种设计入口遵循同一套架构推理逻辑。
> v16（2026-08-24）：补齐“为什么设计”与企业边界。Blueprint 新增 Architecture Brief（目标/用例/约束/数据分级/信任边界/NFR/自治与验收），规则开始结合设计上下文；API 增加深度校验、资源上限、原子写入、乐观并发；提供可配置身份、RBAC、组织/项目隔离；企业 Ontology 扩展批准前要求结构化证据，形成“设计上下文 → 受约束蓝图 → 企业评审证据”闭环。
> v7（2026-08-20）：**从"知识目录"到"架构语言"。** P0 = 架构表达能力：Architecture Relation Model（allowedParents/allowedSiblings/incompatibleWith/dependsOn）+ Decision Record（chosen/alternatives/rejectedReason）+ Responsibility Boundary（owns/not）。风险进一步降级为 Architecture Notes（除非违反硬约束才拦截）。UI 从"设计器"演进为"架构浏览器"（Ontology Explorer / Architecture Canvas / Element Inspector）。**下一步不扩功能，先打磨架构语言——这才是壁垒。**
> v6：回归正向设计流（选架构 → 搭树 → 配节点 → 蓝图），节点 = 架构知识节点，Agent Architecture CAD 定位。
> v5：六问六答定稿（分层交付、Runtime 族、Core/企业两层、多人协作、Multi-Agent 基座）。
> v4：确立 Ontology 为核心壁垒；类型/实例分离。
> v3：十问十答钉死产品边界（web 设计面板、多叉树、相对原子、可运行工程先不做）。

---

## 一、定位

**Agent 架构领域的 CAD——Enterprise Agent Design Tool。不是又一个 Agent 框架，也不是安全治理平台。**

解决的真问题，一句话：

> **现在企业知道怎么部署 Agent，但不知道怎么设计 Agent。**

它不解决（这些都有人解决）：

- 怎么运行 Agent —— Runtime/Harness（dsh、LangGraph）的事
- 怎么写一个 Agent —— 框架和 IDE 的事
- 怎么管理运行中的 Agent —— 远景素材（见第十节 v3）

它解决：

- **如何系统化地描述、设计、评审一个 Agent 系统架构。**

形态类比：

| 领域 | 工具 | 管理对象 |
|---|---|---|
| 集群 | K8s Dashboard | 集群基础设施 |
| 软件工程 | Enterprise Architecture Tool | 软件架构 |
| 工程制造 | CAD | 工程设计 |
| **Agent 系统** | **本平台** | **Agent 系统架构** |

用户（企业级架构设计师）的工作流：

```
设计者（团队协作）
 ↓
Agent Architecture Platform（web 面板）
 ↓
完整架构设计（蓝图：架构树 + 参数 + 边界属性 + 风险消解记录）
 ↓
评审 / 审批 / 修改 / 导出 / 沉淀为组织资产 / 交给实现团队
```

---

## 二、核心论点：壁垒是 Ontology，不是 UI

**K8s 的成功不是因为 Dashboard 漂亮，而是因为它定义了 Pod / Service / Deployment / Ingress 这些抽象。**

同理，UI 容易做，真正壁垒是 **Agent Architecture Ontology（Agent 架构本体）**——Agent 领域的"Kubernetes API"：

```
Harness / Context Engineering / Memory / Planning / Reasoning
Tool System / Multi-Agent / Evaluation / Security / Observability / …
```

Ontology 分两层发行（v5 定稿，详见第四节）：

- **Core Ontology**：平台统一发行，全行业共享——等价于 K8s 核心 API
- **Enterprise Ontology**：企业私有扩展——等价于 CRD，只能挂在 Core 声明的扩展点上

知识不需要发明——ReAct / Multi-Agent / Coding Agent 的结构化知识已散落在论文、GitHub、框架文档、实战项目里，做的是**提取与编码，不是创造理论**。护城河 = Ontology 质量 × 积累时间。

---

## 三、为什么否定"可视化拖拽自由画图"

自由画图（Node + Edge）允许用户设计出**不存在、不可运行、不稳定的架构**：

```
Planner → Memory → Tool → LLM     （看起来合理，工程上全错）
```

- Memory 应挂 Runtime，不应作为流程节点
- Tool 调度应经 Harness，不应箭头直连
- Failure Recovery、子 Agent 生命周期在画布上无处表达

| 自由画图（否定） | 受约束的本体树（本方向） |
|---|---|
| 用户拖 Node + Edge | 用户在 Ontology 约束下勾选/展开 |
| 允许非法结构 | 只能生成合法结构 |
| 产物是一张图 | 产物是结构化蓝图（参数+边界+风险消解） |
| 要求用户懂运行时 | 工程经验编码在 Ontology 里 |

---

## 四、数据模型（v5 定稿）

**类型/实例分离是不可妥协的架构墙；其上叠加四个定稿决策。**

### 4.1 Ontology 层（类型系统）

Agent 架构世界的"类型定义"，`Context Compression` 全库只有一份：

```json
{
  "id": "ctx-compression",
  "namespace": "core",                      // core | enterprise.acme
  "name": "Context Compression",
  "category": "harness.context-engineering",
  "extension_point": false,                 // Core 声明哪些节点接受企业扩展
  "properties": {
    "strategy": { "enum": ["summary", "sliding-window", "hierarchical", "selective-drop"], "default": "summary" },
    "threshold": { "type": "percent", "default": 80 }
  },
  "mitigates": ["context-overflow"],        // 双向绑定：元素侧 → 风险
  "known_problems": ["information-loss", "history-drift"],
  "constraints": {
    "requires": ["context-engineering"],
    "runtime_family": ["event-driven", "stateful-graph"],   // Runtime 族约束
    "suggests": ["compaction-checkpoint"]
  },
  "references": ["dsh packages/compaction", "LangGraph trim_messages"],
  "version": "1.3.0"
}
```

内容 = **知识树 + 架构树 + 参数树三合一**。

### 4.2 Blueprint 层（实例）

```json
{
  "ref": "ctx-compression",
  "params": { "strategy": "hierarchical", "threshold": 70 },
  "reason": "长会话成本高，层级摘要保近期细节",
  "mitigations_applied": ["information-loss → hierarchical-summary"],
  "compliance": "structural"        // 该节点属于规范级还是参考级（继承类型默认，可覆写）
}
```

### 4.3 定稿决策一：分层交付语义（蓝图导出两级）

交给实现团队时，蓝图不是铁板一块：

| 层级 | 语义 | 例子 | 偏离的后果 |
|---|---|---|---|
| **结构性字段** | **MUST（规范）** | 拓扑（supervisor-worker）、元素存在性（挂了 context-isolation） | 偏离 = 架构事故，需重新评审 |
| **参数性字段** | **MAY（参考）** | 压缩比例 70%、重试 3 次 | 实现团队可按实测调整 |

配套语义：**评审与版本也分层**——结构性变更 = major（重走审批），参数调整 = minor（轻量记录）。直觉对齐 semver。

### 4.4 定稿决策二：Runtime 族（设计时锁族，不锁实现）

- Ontology 定义** Runtime 能力族**（event-driven / stateful-graph / stateless-loop…），每族携带 capability profile（supports / forbids 哪些元素）
- 设计时选**族**——它作为约束来源裁剪后续选项；蓝图记录族，**不绑定具体 Runtime**
- 编译期（v3 远景）才解析：`族 + 蓝图 → dsh / LangGraph / 自研`
- 好处：蓝图运行时无关（给"可能替代 Harness"留路）；dsh breaking changes 不伤蓝图层

### 4.5 定稿决策三：Core/企业两层（CRD 模式）

- **Core Ontology**：平台发行、版本化（`ontology.core/v1`），全行业共享
- **Enterprise Ontology**：企业命名空间（`enterprise.acme/v1`），如"企业安全策略"元素，**只能挂在 Core 声明的扩展点上**（`extension_point: true` 的节点）
- 企业扩展同样过 Constraint Engine 与审核；Core 升级时对企业扩展做**兼容性检查**（类似 K8s API 弃用流程）
- 收益：平台掌握行业标准的定义权，企业获得私有定制权，且两者互不污染

### 4.6 定稿决策四：风险消解双向绑定

元素与风险**互为引用**（元素侧 `mitigates` ↔ 风险侧 `mitigations`）：

```
挂载 message-isolation 到蓝图
  → 自动记录「已消解 context-pollution」
展开风险 context-pollution
  → 列出全部可挂载消解元素 → 一键挂载
lint / 审批门禁
  → 高危风险存在未消解 → 警告或阻断
```

蓝图显式携带"消解了什么"的记录（`mitigations_applied`），评审时一目了然。

---

## 五、核心流程：正向设计流（v6 修正）

```
架构师
  ↓
选择目标架构（架构模板：Multi-Agent / RAG / 空白）
  ↓
搭建 Agent 架构树（受约束的多叉树）
  ↓
配置每个节点（参数 / 设计理由）
  ↓
形成完整架构蓝图
  ↓
  ├─→ 查看相关风险（附属视图）
  ├─→ 查看边界问题 / 常见问题（知识卡小节）
  └─→ 查看参考实现（出处）
  ↓
完成架构设计（评审 / 导出 / 沉淀）
```

**反模式（v4~v5 走歪的地方）**：

```
发现风险 → 寻找解决方案 → 生成架构     ✗ 这会把产品变成"风险驱动的安全治理平台"
```

正确关系的例子——设计企业知识库 Agent：

```
选择：Agent 类型 = RAG Agent
展开：RAG Agent ├── Retrieval ├── Embedding ├── Vector Database ├── Reranker └── Generation
配置 Retrieval：strategy = Hybrid
  → BM25: enabled / Vector: BGE-M3 / Fusion: RRF
系统提示（附属知识）：Hybrid Retrieval 常见问题：score normalization、retrieval latency
                     参考：Elasticsearch hybrid search、RRF
```

### 5.1 节点 = 架构知识节点（不是风险节点）

```
Context Compression
 ├── 定义：降低上下文长度的方法
 ├── 实现方式：summary / window / hierarchical
 ├── 适用场景：长任务 Agent
 ├── 参数：compression threshold
 ├── 优缺点：token 下降 / 信息损失增加
 ├── 常见问题：可能导致信息丢失、历史漂移
 └── 参考：dsh packages/compaction
```

对应 Ontology 的层级结构：

```
Ontology
  └── Architecture Element
        ├── Properties（参数 schema）
        ├── Implementations（实现方式）
        ├── Use Cases（适用场景）
        ├── Tradeoffs（优缺点）
        ├── Risks（mitigates / introduces —— 只是附属绑定，不是节点本体）
        └── References（参考实现出处）
```

### 5.2 评价产品的三条标准（v6）

1. **架构表达能力**够不够——能否表达完整的多叉树（Harness / Multi-Agent / Memory / RAG……）
2. **节点知识**够不够丰富——点开节点看到的是知识卡，不是只有风险
3. **设计体验**够不够像 CAD——核心体验是"帮助架构师更快设计复杂系统"，不是"发现错误"

### 5.3 v7：从"知识目录"到"架构语言"（当前主攻方向）

v6 后 OntologyElement 已含知识卡字段，但仍偏"架构百科条目"，缺**架构语义**。差三样东西：

**① Architecture Relation Model** —— 元素间关系是语言的一部分（类似 K8s API schema 定义 Pod 与 Service 的关系）：

```
relations: {
  allowedParents,      # 允许挂载的父元素（超越单一 parentId 的表达）
  allowedSiblings,     # 允许/常见共存的兄弟
  incompatibleWith,    # 互斥（不能出现在同一蓝图）
  dependsOn            # 依赖（存在则必须存在）
}
```

**② Decision Record** —— 企业架构最重要的是"为什么这么设计"，不是"现在是什么"：

```yaml
decision:
  chosen: hierarchical
  alternatives: [sliding-window, summary]
  rejectedReason: 代码上下文不能丢
```

**③ Responsibility Boundary** —— 大型架构不混乱的前提，每个节点声明职责边界：

```yaml
responsibility:
  owns: [任务分解]           # 负责什么
  not: [执行, 验证]           # 不负责什么
```

**优先级（v7）**：

- **P0 架构表达能力**（✅ 完成）：Relation Model + Decision Record + Responsibility（全链路：类型 → lint → 导出 → UI）
- **P1 Ontology 完整度**（✅ 完成）：54 元素全部具备知识卡六件套（定义/实现/适用/优缺点/常见考量/参考）+ relations + 枚举元素均有 alternatives + 4 角色职责模板；质量门单测守护（全元素知识完整度 100% / 关系完整性 / 替代方案 / 职责模板）
- **P2 设计体验**：从"搭积木"到"设计企业 Agent 架构"——UI 演进为架构浏览器（Ontology Explorer / Architecture Canvas / Element Inspector）

**风险降级（v7 定稿）**：风险 = Architecture Notes（"Common considerations: context isolation, communication overhead…"），**不阻断**（除非违反硬约束 requires/taxonomy/family/参数）。架构设计不是漏洞扫描。

## 六、边界与风险系统（降级为附属视图，v6）

> v4 曾把"风险联动"称为面板的灵魂交互——**v6 修正**：设计的主循环是正向搭建；风险系统服务于两个附属时刻：**选中节点时**（知识卡的"常见问题"小节）与**评审时**（消解记录 + 门禁）。它有价值，但不是产品的骨架。

```
选中节点 → 知识卡「常见问题」小节（附属）
提交评审 → 消解记录 + 高危未消解提醒（门禁）
```

- 风险条目（risk → cause → mitigations → trade-offs）仍然结构化存储，与元素双向绑定
- 每条消解标注出处（框架实现/论文/postmortem）
- **本质不变：很多 Agent 工程问题可以通过架构设计使其消失**——但消解发生在架构师正向设计的过程中，而非系统驱动的"补丁式生成"

边界守则：**"评审"做**（架构 lint、消解记录、审批门禁）；**"运行验证"不做**——一做就滑回运行平台。

---

## 七、Ontology 的三个来源，一套治理

不从零创造，优先导入成熟框架作为种子：

```
Framework Template → Ontology Mapping → Architecture Tree
```

| 来源 | 说明 | 治理 |
|---|---|---|
| 框架种子 | LangGraph / dsh / AutoGen 结构化映射 | 映射脚本 + 人工校对，入 Core |
| 人工治理 | 平台维护者对 Core 元素的 CRUD | 审核队列 + 出处标注 + 版本化发行 |
| AI 导入 | 新框架自动结构化入库 | 同一审核队列，防污染 |

企业侧：在 Core 扩展点上建 Enterprise Ontology（内部审核流，平台不管细节，只管兼容性）。

---

## 八、AI + MCP 搭积木（v2，✅ 已交付）

**不是让 AI 自由设计——这是与"AI 帮我设计 Agent（幻觉架构）"的根本区别。**

类比 AI 操作 Kubernetes：它不能凭空创造 Pod 类型，只能 `create deployment / scale replicas`。本平台的 Architecture MCP（`pnpm mcp`，stdio 零依赖）只暴露**受约束的合法操作**——挂载前过 paletteFor、参数过 schema、保存过 lint，所以 AI 不会瞎搞。17 个工具分五组：

```
知识     list_templates / list_families / search_elements / get_element / list_risks
蓝图     list_blueprints / create_blueprint（模板起步）/ get_blueprint（树 + nodeId）
组装     list_palette（受约束调色板）/ add_component / remove_component / set_parameter
语义     set_decision（ADR）/ set_responsibility（职责边界）/ add_comment
交付     validate_blueprint（门禁）/ export_blueprint（分层 YAML）
```

与 web 面板共享同一份存储（`data/` + `ontology/`），AI 搭的蓝图面板立即可见。

AI 只能在 Ontology 圈定的自由度内搭积木——成熟框架约束住了它，不会瞎搞。

---

## 九、最小不可分割单位

**不是 Agent，不是 Harness，而是 Architecture Element（架构元素）——递归对象，原子性相对：**

```
Agent System
└── Harness
    └── Context Engineering
        └── Context Compression
            └── Compression Ratio: 30%
```

树能展开多深取决于 Ontology 当前版本；**可生长性是硬需求**（递归 schema + 版本管理），不存在 K8s Pod 那样的固定原子。

---

## 十、与 deepseek-harness 的关系（已克隆至本目录）

```
本平台（设计）→ 蓝图（运行时无关）→（远期）族+蓝图 → dsh preset cordis.yml
```

- **当前**：dsh 是 Core Ontology 最重要的种子。capability 分包即现成元素表：`core/agent-loop`、`llm/`、`shell/subprocess/terminal`、`e2b/`（沙箱）、`fs/+policy`、**`subagent/`（Multi-Agent 基座的直接种子）**、`session/`（Checkpoint）、`guard`（超时/重试）、`compaction/context`（上下文工程一等公民，验证第八节层级假设）、`preset/`（远期编译对标物）
- **远期**：可能替代 Harness（"有可能会"）。dsh 是 developer preview、breaking 频繁——Runtime 族设计（4.4）正是隔离层。

---

## 十一、产品形态与路线

**首个落地面：Multi-Agent 通用基座（可扩展）。** 不做单一垂直（如 Coding Agent）切入，而是把 Multi-Agent 协作架构做成通用基座——supervisor/worker/communication/shared-state/lifecycle——基座稳定后向垂直领域（coding / RAG / 客服…）扩展，扩展 = 加 Ontology 子树 + 风险条目，不动平台内核。

```
v1（当前焦点）—— 团队级平台，不是单人工具
  web 架构设计面板
  + Ontology v0：Core（Multi-Agent 基座种子）+ 企业扩展点（CRD 模式）
  + 类型/实例分离的蓝图模型（结构=规范 / 参数=参考，分层导出）
  + Runtime 族选择（约束来源，不锁实现）
  + 受约束多叉树设计器 + 风险双向绑定交互
  + 多人协作：组织/角色（架构师/评审人/管理员）、节点级评论批注、
    蓝图状态机（draft → in-review → approved/rejected → versioned）、审批流
  + 蓝图导出（分层 yaml + 可视化）+ 评审 diff（structural/parameter 分级）

v2
  + Architecture MCP（AI 搭积木，见第七节）
  + Ontology 版本升级与企业扩展兼容性迁移
  + 组织级蓝图共享市场（可选）

v3（远期选项，不承诺）
  + 蓝图 → 可运行工程编译（Runtime 族 + 蓝图 → dsh preset / LangGraph）
  + 运行观察 / 反向 MCP / Control Plane / Agent CRD（历史设想归档于此）
  + 可能替代 Harness
```

---

## 十二、风险与注意

1. **壁垒排序清醒**：Ontology > Constraint Engine > 风险库 > 协作流 > UI。资源按此排。
2. **v1 范围警惕**：多人协作（组织/权限/审批）是工程量最大的一块，容易吞掉 Ontology 打磨时间——协作流做薄（状态机+评论+diff 足够），把省下的力气回填 Multi-Agent 基座种子的质量。
3. **Core/企业兼容性是长期税**：Core 每次升级都要跑企业扩展的兼容检查，接口（extension_point）设计要从第一天就稳定。
4. **"验证"的边界**：架构 lint + 审批门禁做，运行验证不做。
5. **类型/实例分离不可妥协**（4.1/4.2），早期偷懒合并，企业共享时必然返工。

---

## 十三、任务清单

> **2026-08-20 已开工**：v1 最小闭环已实现于 `agent-arch/`（core 引擎 + Core Ontology v0 + server + web 面板，15 单测 + 23 冒烟全绿），详见 [agent-arch/README.md](agent-arch/README.md)。以下清单中带 ✓ 的已完成，其余为 v1 迭代/v2 事项。

- [x] Ontology 元素 schema v0 定稿（namespace / extension_point / runtime_family / mitigates / 双向绑定强校验）
- [x] Blueprint schema v0（structural/parameter 分级、mitigations_applied、diff 语义、状态机字段）
- [x] Multi-Agent 基座种子：dsh 包结构 → Ontology 映射（46 元素手工结构化，含出处标注）
- [x] 风险库首批条目（15 条：上下文互串/污染、目标漂移、通信爆炸、记忆碰撞、孤儿子 Agent、失控循环、费用失控、权限升级、提示注入等，含原因/消解/代价/出处）
- [x] Runtime 能力族定义 v0（event-driven / stateful-graph / stateless-loop）+ 族约束过滤
- [x] 协作流最小设计：蓝图状态机（draft→in-review→approved/rejected）+ 审批门禁 + 节点级评论 + 只读保护
- [x] 面板：web 三栏设计器（受约束树 + 调色板 + 风险联动面板 + 校验 + diff + 分层导出）
- [x] Constraint Engine（requires/forbids/suggests/taxonomy/required/参数 schema + 族过滤）
- [x] 蓝图图形化：SVG 渲染器（core，可测试可 API 化）+ 面板“图形”tab + 下载/复制
- [x] 评论 resolve 流转：标记解决/重开 + 只看未解决过滤
- [x] 企业扩展点 UI（CRD 实操）：扩展 tab 在 Core 扩展点上创建/删除企业元素，即时入本体可搭建树；挂载规则由约束引擎守护（非扩展点拒绝、重名拒绝、递归扩展允许）
- [x] 面板打磨三项：蓝图 PNG 导出（客户端 SVG→canvas→PNG，零依赖）、评论 @提及高亮、评论 resolve 流转
- [x] 企业扩展治理：审核队列（提交→pending→批准合并入本体 / 驳回不入本体），治理面板含状态徽章与审批操作
- [x] 企业扩展点 UI（在 Core 扩展点上提交企业元素，挂载受约束引擎守护）
- [ ] 面板打磨保留：树拖拽排序、评论通知推送
- [x] Ontology 版本升级与企业扩展兼容性迁移：schema.json 声明式迁移 + 蓝图版本戳 + GET 自动升级（幂等），rename 递归应用
- [x] 操作审计（企业合规）：audit.jsonl 全通道留痕（HTTP + MCP），actor/action/target/time，面板可查
- [x] 输入校验强化：非法 runtimeFamily/template/nodes 类型全 4xx 拒绝
- [x] CI：GitHub Actions（build + 单测 + 幂等冒烟）
- [x] v2：Architecture MCP（AI 搭积木）——零依赖 stdio 服务，17 个受约束工具，与面板共享存储
- [ ] v2 预研保留：蓝图 → dsh preset cordis.yml 编译（远期）
